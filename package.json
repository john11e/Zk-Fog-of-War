{
  "name": "zero-shot-zk-stellar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:zk": "cd circuits && nargo compile",
    "build:contract": "stellar contract build",
    "deploy": "stellar contract deploy --wasm target/wasm32v1-none/release/zk_game_contract.wasm --source admin --network testnet",
    "start": "bun run dev"
  },
  "dependencies": {
    "@auth0/auth0-react": "^2.15.0",
    "@aztec/bb.js": "^0.30.0",
    "@emailjs/browser": "^4.4.1",
    "@stellar/stellar-sdk": "^14.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "vite": "^5.0.0"
  }
}
